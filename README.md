## Laser Red Laravel To-Do List
Simple 'To Do' list built using Laravel.
### Installation
* Install server-side dependencies by running `composer install`
* Add DB details in `.env`
* Run `php artisan migrate` to create DB tables
* Run `php artisan db:seed` to create dummy tasks
* Install dev dependencies with `npm install`
* Build CSS & JS assets with `gulp build`
